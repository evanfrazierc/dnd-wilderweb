# Validation warns, never blocks

Writes that look invalid (a resource going negative, a building built without its prerequisites) are recorded with a warning, not rejected. The tracker's job is to record what actually happened at the table, including deliberate DM exceptions (see the resource loan in the migrated history) — a hard block would make the tool fight the DM's authority over the campaign.
