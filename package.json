{
  "name": "dnd-wilderweb",
  "version": "0.1.0",
  "private": true,
  "description": "A campaign tracker for the Wilderweb D&D campaign.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "test": "node --test",
    "server": "node server/index.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm:server\" \"npm:client\"",
    "postinstall": "npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run build && node server/index.js",
    "migrate": "node scripts/migrate.js",
    "export": "node scripts/export.js",
    "copy-to-turso": "node scripts/copy-to-turso.js",
    "seed-annual-effects": "node scripts/seed-annual-effects.js"
  },
  "license": "MIT",
  "dependencies": {
    "@libsql/client": "^0.18.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
